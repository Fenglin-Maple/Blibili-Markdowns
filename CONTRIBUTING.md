# 贡献说明

本仓库由“星藏家”生成的 Pull Request 维护。请使用应用的共享工具上传，不要手动复制目录到 `main`。

维护者会检查：

- 文档是否确实来自已完成的 B 站视频总结；
- 路径是否位于提交者自己的 GitHub 数字 ID 目录；
- 稳定文档 ID、BVID、收藏夹来源和多 P 父子关系是否一致；
- 是否包含原始媒体、Cookie、密钥、数据库、绝对路径或不安全链接；
- Markdown 图片资源是否完整、大小是否合理。

Pull Request 通过 GitHub Actions 的结构校验后，再由维护者人工审核。校验通过不代表内容自动合并。

## 不同总结共存

相同 BVID 的不同总结可以同时存在。不同贡献者、B 站账户、收藏夹、模型或提示词都可能产生不同内容；请不要为了“去重”删除或覆盖其它文档。

## 更新已上传文档

应用会沿用稳定 `documentId` 提交更新。维护者审核更新时，请确认变更仍属于同一视频和同一来源命名空间；如果来源发生变化，应作为新文档提交。

